package tyrian.cmds

class RandomTests extends munit.FunSuite {

  // import tyrian.CmdUtils.*

  // test("NextInt") {
  //   val actual = Random.Seeded(2).int(5).run.value
  //   assert(clue(actual) >= 0 && actual < 5)
  // }

  // test("NextLong") {
  //   val actual = Random.Seeded(2).long(5).run.value
  //   assert(clue(actual) >= 0 && actual < 5)
  // }

  // test("NextFloat") {
  //   val actual = Random.Seeded(2).float.run.value
  //   assert(clue(actual) >= 0.0f && actual <= 1.0f)
  // }

  // test("NextDouble") {
  //   val actual = Random.Seeded(2).double.run.value
  //   assert(clue(actual) >= 0.0d && actual <= 1.0d)
  // }

  // test("NextAlphaNumeric") {
  //   val actual = Random.Seeded(2).alphaNumeric(5).run.value
  //   assert(clue(actual.length) <= 5)
  //   assert(clue(actual) == "oC8rH")
  // }

  // test("NextShuffle") {
  //   val actual = Random.Seeded(2).shuffle(List(1, 2, 3)).run.value
  //   assert(clue(actual) == List(3, 1, 2))
  // }

}
