{
  "scripts": {
    "start": "sbt fastOptJS && parcel index.html --no-cache --out-dir dist --log-level 4 --no-source-maps",
    "build": "sbt clean fastOptJS && parcel build index.html --out-dir dist --log-level 4 --no-source-maps"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.34.0"
  },
  "dependencies": {
    "bootstrap": "^5.0.1",
    "popper.js": "^1.16.1",
    "snabbdom": "^3.0.1"
  }
}
