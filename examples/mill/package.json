{
  "scripts": {
    "start": "mill counter.buildSite && parcel index.html --no-cache --out-dir dist --log-level 4 --no-source-maps",
    "build": "mill clean counter && mill counter.buildSite && parcel build index.html --out-dir dist --log-level 4 --no-source-maps"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {
    "snabbdom": "^3.0.1"
  }
}
