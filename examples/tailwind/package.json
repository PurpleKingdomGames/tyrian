{
  "source": "src/main/html/index.html",
  "scripts": {
    "start": "parcel --no-cache --dist-dir dist --log-level info",
    "build": "parcel build --dist-dir dist --log-level info"
  },
  "devDependencies": {
    "parcel": "^2.1.0",
    "postcss": "^8.4.38",
    "process": "^0.11.10",
    "tailwindcss": "^3.4.1"
  }
}
